# ============================================================================
# AC FIXBOT - Configuracion de Secretos
# ============================================================================
# Copia este archivo a config.env y completa los valores
# NUNCA subir config.env a git (ya esta en .gitignore)
#
# NOTA: La configuracion de infraestructura (nombres, SKUs, etc.) esta en:
#   environments/dev.env   - Desarrollo
#   environments/tst.env   - Testing
#   environments/prod.env  - Produccion
#
# Este archivo solo contiene SECRETOS que no deben estar en git
# ============================================================================

# ----------------------------------------------------------------------------
# SQL SERVER
# ----------------------------------------------------------------------------
# Password seguro: minimo 12 chars, mayusculas, minusculas, numeros, simbolos
SQL_ADMIN_USER="sqladmin"
SQL_ADMIN_PASSWORD=""

# ----------------------------------------------------------------------------
# WHATSAPP BUSINESS API
# Obtener de: https://developers.facebook.com > Tu App > WhatsApp
# ----------------------------------------------------------------------------
WHATSAPP_TOKEN=""
WHATSAPP_PHONE_ID=""
WHATSAPP_VERIFY_TOKEN=""
WHATSAPP_APP_SECRET=""

# ----------------------------------------------------------------------------
# INTELIGENCIA ARTIFICIAL
# ----------------------------------------------------------------------------
# Proveedor: gemini (POC/dev) o azure-openai (produccion)
AI_PROVIDER="gemini"
USE_AI="true"

# Google Gemini (gratis para desarrollo)
# Obtener de: https://aistudio.google.com/apikey
GEMINI_API_KEY=""

# Azure OpenAI (solo si usas azure-openai como AI_PROVIDER)
# Se habilita con ENABLE_AZURE_OPENAI=true en el env del ambiente
ENABLE_AZURE_OPENAI="false"
AZURE_OPENAI_LOCATION="eastus"

# Azure OpenAI Whisper (transcripcion de audio)
# Cuenta separada en northcentralus por disponibilidad regional
ENABLE_WHISPER_MODEL="false"
AZURE_WHISPER_LOCATION="northcentralus"

# ----------------------------------------------------------------------------
# ADMIN API
# ----------------------------------------------------------------------------
# La autenticacion usa Azure Function Keys (no requiere config manual)
# Las keys se gestionan en: Azure Portal > Function App > App Keys

# ----------------------------------------------------------------------------
# RECURSOS OPCIONALES
# ----------------------------------------------------------------------------
# Estos flags controlan si deploy.sh despliega y configura estos recursos.
# Deben coincidir con los params en infra/parameters/<env>.bicepparam
ENABLE_REDIS="false"
ENABLE_SERVICEBUS="false"

# ----------------------------------------------------------------------------
# ALERTAS (Opcional - Slack/Teams webhook)
# ----------------------------------------------------------------------------
# ALERT_WEBHOOK_URL=""

# ============================================================================
# DESPUES DEL DEPLOY:
# Los secretos se guardan automaticamente en Azure Key Vault
# y la Function App los accede via Managed Identity
# ============================================================================
